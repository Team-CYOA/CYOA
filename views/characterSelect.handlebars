<section class="section">
    <div class="container">
        <div class="column is-centered">
            <div class="column is-half">
                <h1> Characters! </h1>
                <ul>
                    {{#each this.chars}}
                    <div><button id="current encounter row id here" class="button is-medium is-full-length is-hovered is-rounded">{{this.name}}</button></div>
                    {{/each}}
                </ul>
            </div>
        </div>
    </div>
</section>

<!--activChar needs a "current event" column-->